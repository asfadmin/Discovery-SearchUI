import { async } from '@angular/core/testing';
import {DrawService} from './draw.service';

describe('DrawService', () => {
  let service;

  beforeEach(() => {
    service = new DrawService();
  });

  it('should run #getLayer()', async () => {
    // const result = getLayer();
  });

  it('should run #setInteractionMode()', async () => {
    // const result = setInteractionMode(map, mode);
  });

  it('should run #setDrawMode()', async () => {
    // setDrawMode(map, mode);
  });

  it('should run #setInteraction()', async () => {
    // setInteraction(mode);
  });

  it('should run #setDrawStyle()', async () => {
    // setDrawStyle(style);
  });

  it('should run #setFeature()', async () => {
    // setFeature(feature, epsg);
  });

  it('should run #create()', async () => {
    // const result = create(drawMode);
  });

  it('should run #createModify()', async () => {
    // const result = createModify();
  });

  it('should run #setValidStyle()', async () => {
    // setValidStyle();
  });

  it('should run #setOmittedStyle()', async () => {
    // const result = setOmittedStyle();
  });

  it('should run #setInvalidStyle()', async () => {
    // setInvalidStyle();
  });

});
