import { async } from '@angular/core/testing';
import {WktService} from './wkt.service';

describe('WktService', () => {
  let service;

  beforeEach(() => {
    service = new WktService();
  });

  it('should run #wktToFeature()', async () => {
    // const result = wktToFeature(wkt, epsg);
  });

  it('should run #featureToWkt()', async () => {
    // const result = featureToWkt(feature, epsg);
  });

});
