import { async } from '@angular/core/testing';
import {RangeService} from './range.service';

describe('RangeService', () => {
  let service;

  beforeEach(() => {
    service = new RangeService();
  });

  it('should run #toString()', async () => {
    // const result = toString(range);
  });

});
