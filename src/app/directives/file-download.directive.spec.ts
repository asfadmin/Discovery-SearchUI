import { FileDownloadDirective } from './file-download.directive';

describe('FileDownloadDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDownloadDirective();
    expect(directive).toBeTruthy();
  });
});
